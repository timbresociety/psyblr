{
  "name": "@psyblr/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker and Durable Object backend scaffold for Psyblr.",
  "dependencies": {
    "@psyblr/game-engine": "file:../../packages/game-engine"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.4",
    "wrangler": "^4.0.0"
  }
}
